import React from 'react';
import Logo from '../../solid/logo/logo';
import Navigation from '../../solid/navigation/Navigation';

const Header = () => {

    return(
        <header>
            <Logo/>
            <Navigation/>
        </header>
    )
}

export default Header;
